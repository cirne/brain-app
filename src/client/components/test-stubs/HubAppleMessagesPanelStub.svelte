<script lang="ts">
  type Props = {
    onClosePanel?: () => void
  }
  // eslint-disable-next-line no-empty-pattern -- intentional: props optional for API parity
  let {}: Props = $props()
</script>

<div data-testid="hub-apple-messages-stub">HubAppleMessagesPanel</div>
