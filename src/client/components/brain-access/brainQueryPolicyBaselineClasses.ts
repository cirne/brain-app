/** Shared `<pre>` styling for baseline-list and read-only policy text in Brain Access. */
export const baselineListPreClass =
  'max-h-[min(40vh,16rem)] overflow-auto whitespace-pre-wrap break-words rounded border border-[color-mix(in_srgb,var(--border)_35%,transparent)] bg-surface p-2 font-mono text-[0.625rem] leading-relaxed text-muted'
