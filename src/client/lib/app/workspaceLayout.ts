/**
 * Side-by-side chat + detail vs slide-over uses the measured width of the chat workspace column
 * (the main flex area next to the history sidebar), not the full viewport.
 *
 * Same breakpoint family as `(max-width: 767px)` / `(min-width: 768px)` in CSS.
 */
export const WORKSPACE_DESKTOP_SPLIT_MIN_PX = 768
