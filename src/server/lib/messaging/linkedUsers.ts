import {
  listLinkedUsersInWorkspace,
  resolveLinkedTenant,
  type SlackUserLinkRow,
} from '@server/lib/slack/slackConnectionsRepo.js'

export { listLinkedUsersInWorkspace, resolveLinkedTenant }
export type { SlackUserLinkRow }
