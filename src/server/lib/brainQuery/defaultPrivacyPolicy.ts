import { BRAIN_QUERY_BUILTIN_POLICY_BODIES } from '@shared/brainQueryBuiltinPolicyBodies.js'

/** Legacy name: same text as built-in id `server-default` (see `brain-query/privacy/server-default.hbs`). */
export const DEFAULT_BRAIN_QUERY_PRIVACY_POLICY = BRAIN_QUERY_BUILTIN_POLICY_BODIES['server-default']
