services:
  brain:
    build: .
    ports:
      - "4000:4000"
    env_file: .env
    environment:
      # Override local paths from .env with container paths
      - NODE_ENV=production
      - PORT=4000
      - WIKI_DIR=/wiki
      - RIPMAIL_BIN=ripmail
      - RIPMAIL_HOME=/ripmail
