# Bugs — Agent and User Reports

When an agent or user hits a failure, we document it here. Root cause and "agent-intuitive" implications matter: **is the CLI intuitive enough for the LLM?** See [VISION.md](./VISION.md) (agent-first, agent-intuitive interfaces).

**Monorepo (brain-app):** Bugs for the **ripmail** CLI and local index belong here (`ripmail/docs/bugs/`). Bugs in the **brain-app** server or UI (Hono, Svelte, Tauri shell, wiki routes) are tracked in **[`../../docs/BUGS.md`](../../docs/BUGS.md)**. `BUG-*` IDs are **not** aligned between the two files. See also **[`../../docs/OPPORTUNITIES.md`](../../docs/OPPORTUNITIES.md)** for app-level opportunities.

**Distribution:** **Rust** at the repo root is the **only** in-repo implementation and CLI (`cargo build` / prebuilt binary). Historical TypeScript lived under **`node/`** until cutover ([ADR-025](ARCHITECTURE.md#adr-025-rust-port--parallel-implementation-pre-cutover), [OPP-030 archived](opportunities/archive/OPP-030-rust-port-cutover.md), [RUST_PORT.md](RUST_PORT.md)).

---

## Active

| ID | Title | Summary |
|---|---|---|
| [BUG-022](bugs/BUG-022-eval-judge-rejects-fixture-dates.md) | Eval Judge Rejects Correct Answers Due to Fixture Dates | The eval LLM judge considers 2026 fixture dates "future" and penalizes correct answers. "What emails did I get today?" scores 0.40–0.60 despite listing the right emails. Workaround: `minScore: 0.3` on affected test. Fix: add fixture context to judge prompt or use deterministic checks. |
| [BUG-026](bugs/BUG-026-who-nicknames-i18n-and-query-contract.md) | `who` — English nickname map, Rust parity, no multi-term query | Large hand-maintained English nickname dictionary for identity merge + matching; inaccurate for i18n and ambiguous forms. Rust `who` ships a small subset → parity gap. No OR / multi-variant query string (`tom|thomas` is literal substring). Fix options: external data, shared TS/Rust source, explicit `--terms`/multi-call docs, agent skill guidance. |
| [BUG-033](bugs/BUG-033-actionable-file-not-found-errors.md) | Raw File/OS Errors Are Not Actionable Enough for Agents | `read` and some send paths still surface low-level `os error 2` or path-oriented failures instead of ripmail-specific errors with suggested recovery steps like `ripmail sync`. This makes agent recovery logic brittle and inconsistent across commands. |
| [BUG-038](bugs/BUG-038-stats-inaccurate-threads-and-people.md) | `ripmail stats` — threads = messages, people always 0 | `collect_stats` counts `threads` and `people` tables. Persist sets `thread_id` = `message_id`, so one thread row per message; `people` is never populated by sync. Misleading for agents/users expecting real thread and contact counts. |
| [BUG-053](bugs/BUG-053-wizard-llm-provider-selection.md) | Wizard/setup do not configure LLM provider or models | [OPP-046 archived](opportunities/archive/OPP-046-llm-provider-flexibility.md) added `llm` in `config.json` + env for OpenAI/Anthropic/Ollama, but `ripmail wizard` still only collects an OpenAI key. Anthropic/Ollama users must edit JSON/env by hand. Extend wizard shared settings and `ripmail setup` flags per BUG doc. |
| [BUG-056](bugs/BUG-056-wizard-bad-password-exits-instead-of-retry.md) | Wizard exits on bad IMAP password instead of retrying | When the user enters a wrong app password, `ripmail wizard` prints "Could not connect" and exits to the shell instead of looping back to re-prompt. User must restart the wizard from scratch to correct a typo. Fix: catch auth failure in the credential loop and re-prompt for the password. |
| [BUG-061](bugs/BUG-061-google-drive-sync-hang-search-empty.md) | Google Drive source — sync hangs, `search` empty while list works | Experimental work on branch `google-drive`. Drive file listing works; local sync/refresh does not complete reliably and `ripmail search` finds no Drive content. May drive a pivot toward Drive-native search in tools vs full local FTS. See bug doc and [brain-app OPP-045](../../docs/opportunities/OPP-045-cloud-file-sources-drive-dropbox.md). |

---

## Fixed (archived)

Fixed bugs are kept for context in [bugs/archive/](bugs/archive/).

| ID | Title | Summary |
|---|---|---|
| [BUG-060](bugs/archive/BUG-060-search-index-misses-receipt-text-in-html-only-bodies.md) | Search missed receipt/order text in HTML-only or stub-plain bodies | **Fixed (2026-04-22).** Indexing used `text/plain` whenever present; many receipt emails (e.g. Shopify) put the real copy in HTML. `indexable_bodies()` in `parse_message.rs` now falls back to or prefers HTML-derived text when plain is empty/whitespace or a short stub. Re-sync to refresh existing rows. |
| [BUG-055](bugs/archive/BUG-055-imap-archive-slow.md) | `ripmail archive` with IMAP can take tens of seconds | **Closed (2026-04-22).** Mitigations: fast Gmail search, skip blocking `LOGOUT`, OPP-049 `UID STORE` + timing logs; **accepted** large-mailbox `SELECT [Gmail]/All Mail` latency. Deferred: daemon/pool, batch archive. Enable server archive via `ripmail config --mailbox-management on`. See [GMAIL_ARCHIVE_NOTES.md](GMAIL_ARCHIVE_NOTES.md). |
| [BUG-058](bugs/archive/BUG-058-whoami-wrong-identity-shared-mail.md) | `whoami` infers wrong identity on shared Apple Mail | When multiple household members share Mail.app, `ripmail whoami` could infer the wrong primary identity (whoever sends the most email). Root cause: broken SQL/bindings in the recipient-side candidate query in `who_infer.rs`, plus Sent-frequency primary election. **Fixed:** valid `UNION` + doubled mailbox binds; receiver-frequency primary among candidates. See [BUG-058](bugs/archive/BUG-058-whoami-wrong-identity-shared-mail.md). |
| [BUG-057](bugs/archive/BUG-057-applemail-sync-new-indexed-zero.md) | Apple Mail sync `new_indexed=0` despite processing messages | **Fixed (2026-04-15).** `persist_message` uses `INSERT` with explicit duplicate handling (not silent `INSERT OR IGNORE`); batched `uids_already_indexed` for Apple Mail dedup; see bug doc. |
| [BUG-054](bugs/archive/BUG-054-gmail-imap-archive-noop.md) | Gmail IMAP archive was a no-op (All Mail → All Mail MOVE) | **Fixed (2026-04-14).** Gmail-specific `UID STORE` / INBOX search + MOVE; see [GMAIL_ARCHIVE_NOTES.md](GMAIL_ARCHIVE_NOTES.md). |
| [BUG-017](bugs/archive/BUG-017-semantic-recall-gap-intent-queries.md) | Semantic Recall Gap for Intent-Based Queries — Agent-Reported | **Won't Fix (2026-04-10).** Resolution path confirmed by Bakeoff #5: FTS + agent query decomposition addresses this. Root cause is semantic search as default, not fundamental search recall problem. See [OPP-019 archived](opportunities/archive/OPP-019-fts-first-retire-semantic-default.md). |
| [BUG-020](bugs/BUG-020-apple-spending-domain-from-routing.md) | Apple / Vendor Spending Query — Domain Not Routed to fromAddress | **Fixed (2026-04-10).** Implemented backend domain→from routing in `parse_search_query()`. When domain pattern detected in query and no explicit `fromAddress` set, automatically route to filter. Domain detection happens after operator stripping to avoid false matches. 4 new tests confirm fix. |
| [BUG-032](bugs/archive/BUG-032-search-query-should-be-optional-with-filters.md) | `ripmail search` Should Allow Filter-Only Queries Without Dummy Text | **Fixed (2026-04-10).** `<QUERY>` positional arg changed to `Option<String>`; `run_search` validates at least one constraint present. 3 new parser tests. |
| [BUG-042](bugs/archive/BUG-042-search-fts5-special-characters-error-ux.md) | Search — raw FTS5/SQLite errors for `&` and reserved chars | **Fixed (2026-04-11).** FTS escaping (`src/search/escape.rs`), clearer engine errors; integration `fts_ampersand_plus_query_runs` in `tests/search_fts.rs`. |
| [BUG-043](bugs/archive/BUG-043-rfc-2047-subject-not-decoded-cli-json.md) | RFC 2047 subjects not decoded in `--text` or JSON | **Fixed (2026-04-11).** `src/mime_decode.rs` wired through read/search/inbox paths; unit tests in same module. |
| [BUG-044](bugs/archive/BUG-044-attachment-list-ignores-message-id-flag.md) | `attachment list` ignores `--message-id` | **Fixed (2026-04-11).** Positional message id + `--message-id` / `-m` in `src/cli/args.rs` and `mail.rs`. |
| [BUG-045](bugs/archive/BUG-045-draft-reply-positional-message-id-inconsistent.md) | `draft reply`/`forward` message id inconsistency | **Fixed (2026-04-11).** Positional `<message-id>` aligned with `read`; see `DraftCmd` + `tests/draft_lazy_db.rs`. |
| [BUG-046](bugs/archive/BUG-046-ask-time-relative-queries-favor-stale-mail.md) | `ask` favors stale mail for time-relative questions | **Fixed (2026-04-11).** `sanitize_search_args` injects `afterDate: 90d` when cues match and no dates set; unit tests in `src/ask/agent.rs`. |
| [BUG-047](bugs/archive/BUG-047-oauth-refresh-missing-embedded-client-and-path-leak.md) | OAuth errors — embedded client + path leak | **Fixed (2026-04-11).** `src/oauth/client.rs` diagnostics without CI path leakage. |
| [BUG-048](bugs/archive/BUG-048-inbox-no-window-misleading-empty-reason.md) | Inbox empty `no_local_mail` vs window | **Fixed (2026-04-11).** `emptyReason` `no_mail_in_window` vs `no_local_mail`; tests in `src/refresh.rs` (`inbox_json_hints_tests`). |
| [BUG-049](bugs/archive/BUG-049-search-from-operator-plus-keywords-returns-empty.md) | `from:` + keywords returned empty | **Fixed (2026-04-11).** Keyword + sender intersection; `search_from_operator_plus_keywords_intersects` in `tests/search_fts.rs`. |
| [BUG-050](bugs/archive/BUG-050-search-unsupported-operators-leak-sql-errors.md) | Unsupported operators leaked SQL errors | **Fixed (2026-04-11).** `validate_search_query_operators` in `src/search/query_parse.rs`; `search_rejects_unknown_attachment_operator` in `tests/search_fts.rs`. |
| [BUG-051](bugs/archive/BUG-051-sync-connection-lost-error-vague.md) | Vague sync disconnect message | **Fixed (2026-04-11).** `enrich_imap_disconnect_message` in `src/sync/error.rs`; unit tests in same module. |
| [BUG-052](bugs/archive/BUG-052-rules-remove-yes-flag-ignored.md) | `rules remove --yes` ignored | **Fixed (2026-04-11).** `-y` / `--yes` on `rules remove`; parser test `rules_remove_parses_yes_short_flag` in `src/cli/args.rs`. |
| [BUG-054](bugs/archive/BUG-054-body-flag-leaks-into-sent-email.md) | `--body` leaks into draft body and sent mail | **Fixed (2026-04-12).** `strip_leading_cli_body_flag` in `draft_body.rs` (`write_draft` + `draft_markdown_to_plain_text`); drop leading `--body` tokens from `draft edit` / `draft rewrite` trailing args in `draft.rs`; tests in `draft_body`, `draft_store`, `args`, `draft`. |
| [BUG-041](bugs/archive/BUG-041-sent-mail-literal-backslash-n-in-body.md) | Sent mail body shows literal `\n` instead of line breaks | **Fixed (2026-04-08).** `normalize_smtp_plain_body` in `src/send/smtp_send.rs` before MIME send; unit tests in `smtp_send.rs`. **Also:** QP soft-wrap must not drop real paragraph breaks after normalization — see archive note + `formatted_body_round_trips_literal_backslash_n_then_qp`. |
| [BUG-040](bugs/archive/BUG-040-oauth-mailbox-draft-send-smtp-blocked.md) | OAuth mailboxes — `draft` / `send` blocked by IMAP password gate | **Fixed (2026-04-06).** Removed `run_draft` password gate; `run_send` uses `smtp_credentials_ready` (OAuth token file vs app password). SMTP XOAUTH2 already in `send_simple_message`. Tests: `tests/oauth_mailbox_draft_send_cli.rs`. |
| [BUG-039](bugs/archive/BUG-039-status-imap-empty-sync-mailbox-multi-inbox.md) | `ripmail status --imap` — invalid folder when `sync.mailbox` empty | **Fixed (2026-04-05).** `get_imap_server_status` uses `resolve_sync_mailbox(cfg)` for IMAP STATUS and `sync_state` folder key (same as sync). Tests: `resolve_sync_mailbox_*` in `src/sync/run.rs`. |
| [BUG-037](bugs/archive/BUG-037-search-parenthesized-or-filters-break-fts.md) | Parenthesized OR Across `from:`/`to:` Filters Broke FTS | **Fixed (2026-04-05).** Parser + SQL: `from:x OR to:y` (optional parens) now unions address filters and ANDs FTS; `from`/`to` token regex no longer eats `)`; redundant outer `()` around keyword clause stripped for FTS5. Tests in `query_parse`, `search_fts`. |
| [BUG-025](bugs/archive/BUG-025-mcp-cli-parity-deferred.md) | MCP/CLI Parity — Superseded | **Closed — superseded (2026-04-05).** MCP stdio server removed from tree; parity bug moot until [OPP-039](opportunities/OPP-039-mcp-deferred-cli-first.md). |
| [BUG-024](bugs/archive/BUG-024-inbox-scan-over-filters-misses-important-mail.md) | Inbox scan over-filters — misses important mail (was `ripmail check`; now `ripmail inbox`) | **Closed — verified (2026-04-03).** LLM-inbox era: prompt + stripper fixes. **Follow-up:** inbox is **deterministic** ([OPP-037 archived](opportunities/archive/OPP-037-typed-inbox-rules-eval-style.md)); tune **`rules.json`** / defaults if similar recall issues appear. |
| [BUG-035](bugs/archive/BUG-035-read-omits-to-cc-bcc-and-threading-headers.md) | `ripmail read` Omits To/CC/BCC and Related Headers | Fixed (Rust 2026-04-01): `ripmail read` default text prints From/To/Cc/Bcc/Reply-To/Date/Subject/Message-ID/In-Reply-To/References + body; `ripmail read --json` emits structured `to`/`cc`/`bcc` (`name`+`address`), `replyTo`, threading, `recipientsDisclosed`, `body`. |
| [BUG-036](bugs/archive/BUG-036-pdf-attachments-non-ascii-filename-mime-parse.md) | PDF / Attachments Missing When Filename Has Non-ASCII (MIME parse) | Fixed (2026-04-03): UTF-8 in quoted `filename=` tolerated; `parse_raw_message` + attachment fallback names; regression tests in `tests/attachments_extract.rs`, `tests/sync_parse_maildir.rs`; Node parity in `parse-message.ts`. |
| [BUG-001](bugs/archive/BUG-001-attachment-and-read-agent-friction.md) | Attachment and Read/Thread Friction — Agent-Reported | Read/thread ID handling, attachment read argument order, extract vs download (`--raw`), PDF in binary. Fixed (PDF via OPP-007; other items addressed or documented). |
| [BUG-002](bugs/archive/BUG-002-attachment-discoverability-and-read.md) | Attachment Discoverability and Read — Agent-Reported | Attachment subcommands (`attachment list`, `attachment read`) not discoverable from help; `read` doesn't show attachments; no path from "extracted: true" to content. Fixed: `read` now shows attachment summary with hints, attachment commands added to help. |
| [BUG-003](bugs/archive/BUG-003-fts-vs-semantic-search-guidance.md) | FTS vs Semantic Search Guidance — Agent-Reported | `--mode` flag exists but lacks discoverability and usage guidance. **Superseded by [OPP-008](opportunities/archive/OPP-008-simplify-search-modes.md)** — simplified interface by making hybrid default and removing mode selection complexity entirely. |
| [BUG-005](bugs/archive/BUG-005-xlsx-formula-cells-object-object.md) | XLSX Formula Cells Render as `[object Object]` — Agent-Reported | XLSX cells with formulas render as `[object Object]` instead of computed values; USD/totals lost in extracted CSV. Fixed: handle formula cell objects by extracting computed result from `result`, `value`, or `w` properties. |
| [BUG-006](bugs/archive/BUG-006-sync-repeated-connecting-message.md) | Sync Repeated "Connecting" Message in Non-TTY Mode — Agent-Reported | `ripmail sync` printed "Connecting to IMAP server..." repeatedly in non-TTY mode. Fixed: guard with `process.stdout.isTTY`, print each status line only once in non-TTY. |
| [BUG-007](bugs/archive/BUG-007-sync-silent-auth-failure.md) | Sync Silent Authentication Failure — Agent-Reported | `ripmail sync` reported success (exit 0) with invalid credentials; background sync crashed silently during IMAP auth, no error surfaced. Fixed: pre-auth IMAP validation in foreground before spawning background sync; auth failures caught immediately (exit 1, no background process spawned). |
| [BUG-009](bugs/archive/BUG-009-wizard-crash-non-interactive.md) | `ripmail wizard` Crashes with Stack Trace on Non-Interactive Stdin — Agent-Reported | `ripmail wizard` crashed with unhandled `ExitPromptError` and full stack trace when run in non-TTY mode. Fixed: added TTY check at start of wizard function, exits gracefully with clear error message. |
| [BUG-004](bugs/archive/BUG-004-attachment-read-silent-failure.md) | Attachment Read Silent Failure — Agent-Reported | `ripmail attachment read` fails silently (exit 1, no output) when given invalid index (e.g. 0). Help text doesn't clarify 0-based vs 1-based. Fixed (Verified 2026-03-07): Clear error message indicates 1-based indexing and valid range. |
| [BUG-008](bugs/archive/BUG-008-who-case-sensitive-email-dedup.md) | `ripmail who` Case-Sensitive Email Deduplication — Agent-Reported | `ripmail who` treats email addresses as case-sensitive, creating duplicate entries for the same person when addresses appear in different cases. Fixed (Verified 2026-03-07): All addresses normalized to lowercase, no more case-variant duplicates. |
| [BUG-010](bugs/archive/BUG-010-sync-backward-resume-skips-date-range.md) | Sync Backward Resume Skips Requested Date Range — Agent-Reported | `ripmail sync --since 90d` after a 7-day sync silently skips the older date range; backward resume uses `oldestSynced` instead of `requestedSince` as the IMAP SEARCH boundary. Fixed: use `fromDate` (requested date) instead of `oldestDateStr` when expanding sync range. |
| [BUG-011](bugs/archive/BUG-011-who-dartmouth-not-merged.md) | `lewis.cirne@alum.dartmouth.org` Not Merged with Lewis Cirne Identity — Agent-Reported | Identity merger misses addresses with local-part variations (dots, underscores) when no display name exists. Fixed (Verified 2026-03-07): Name inference and fuzzy local-part matching enable merging of dot/underscore variants. |
| [BUG-012](bugs/archive/BUG-012-who-min-sent-splits-identity.md) | `--min-sent` Filter Splits Merged Identities — Agent-Reported | Filters apply before identity merging, causing merged identities to be split when individual addresses fall below threshold. Fixed (Verified 2026-03-07): Filters now apply after merging and aggregation. |
| [BUG-013](bugs/archive/BUG-013-who-noreply-display-name-leaks.md) | Noreply Addresses Leak Through Filter When Display Name Matches Query — Agent-Reported | Noreply addresses with matching display names bypass the noreply filter, appearing in results without `--all`. Fixed (Verified 2026-03-07): Noreply filter checks both addresses and display name patterns. |
| [BUG-014](bugs/archive/BUG-014-who-signature-parser-noise.md) | Signature Parser Extracts Footer Boilerplate as Title/Company — Agent-Reported | Signature parser extracts copyright notices, mailing addresses, and tracking URLs as contact information. Fixed (Verified 2026-03-07): Boilerplate filtering and noreply address skipping implemented. Additional improvements: standalone company name extraction, quoted reply detection, better signature selection (verified 2026-03-07). |
| [BUG-015](bugs/archive/BUG-015-who-name-inference-noreply-garbage.md) | Name Inference Produces Garbled `aka` Values for Noreply Addresses — Agent-Reported | Name inference runs on noreply addresses, producing garbled names like "Mess Ages-noreply" from `messages-noreply@linkedin.com`. Fixed (Verified 2026-03-07): Name inference skipped for noreply/bot addresses. |
| [BUG-016](bugs/archive/BUG-016-bakeoff-incomplete-coverage-critical.md) | Bakeoff Failure — Incomplete Coverage vs Google Adapter — Agent-Reported | Archived 2026-03-10. Superseded by BUG-020 for domain→from / apple spending. Other items addressed via instructions, tool docs, noise-excluded-by-default, eval known-issue. |
| [BUG-018](bugs/archive/BUG-018-who-timings-unknown-flag.md) | `ripmail who --timings` Unknown Flag — Agent-Reported | Fixed: `--timings` flag added to `who` command, consistent with `search --timings`. Timing only included when flag is passed. |
| [BUG-019](bugs/archive/BUG-019-refresh-missed-new-messages.md) | Refresh Reports No New Messages Despite New Mail in Inbox | Fixed: `--force` flag skips STATUS early exit; clear messaging when early-exit occurs; refresh output includes `earlyExit` field. |
| [BUG-021](bugs/archive/BUG-021-read-prepare-error.md) | `ripmail read` Fails with Database Prepare Error | Fixed: all call sites now pass `db` explicitly to `formatMessageForOutput` instead of relying on internal `getDb()` fallback that failed due to module initialization issues. |
| [BUG-023](bugs/archive/BUG-023-attachments-missing-from-synced-email.md) | Attachments Missing from Synced Email | Fixed (2026-03-09): Attachment filter incorrectly dropped attachments with `disposition: "attachment"` when postal-mime set `related: true` (due to nested multipart structures). Updated filter to preserve explicit attachments regardless of `related` flag. Recovered 14 attachments across 6 messages. |
| [BUG-027](bugs/archive/BUG-027-rust-draft-cli-errors-and-stdin-hang.md) | Rust `ripmail draft` — opaque missing-draft errors, stdin block | Fixed (2026-03-30): Missing draft id maps to Node-style message (`format_draft_not_found_message` / `read_draft_in_data_dir` `NotFound`). `draft new --to` only: placeholder draft, no LLM stdin block (see archived doc). |
| [BUG-028](bugs/archive/BUG-028-inbox-text-utf8-snippet-panic.md) | `ripmail inbox --text` UTF-8 panic at snippet wrap | Fixed (2026-03-30): `wrap_line` uses `floor_char_boundary` before prefix slice; regression tests in `src/refresh.rs`. |
| [BUG-029](bugs/archive/BUG-029-read-bare-message-id-no-angle-brackets.md) | Bare Message-ID / thread ID without angle brackets → lookup failed | Fixed (2026-03-31): `src/ids.rs` resolves `<id>` then bare `id`; wired through read, thread, attachments, draft, ask. |
| [BUG-030](bugs/archive/BUG-030-draft-commands-hang-after-edit.md) | Draft commands hang after `draft edit` | **Closed — verified (2026-03-31).** Lazy SQLite open (draft list/view/edit; forward send). See [ADR-026](ARCHITECTURE.md#adr-026-sqlite-concurrency--reads-vs-writes-lazy-open-avoiding-lock-contention). |
| [BUG-031](bugs/archive/BUG-031-send-reply-draft-wrong-maildir-path.md) | `ripmail send <draft-id>` Fails for Reply Drafts Due to Wrong Maildir Path | Fixed and verified (2026-03-31): shared maildir path resolution now handles legacy `cur/...` rows and canonical `maildir/cur/...` rows, restoring reply draft send threading. |
| [BUG-034](bugs/archive/BUG-034-cli-json-text-flags-inconsistent-across-subcommands.md) | CLI Rejected `--json` on `search` and `--text` on `thread` | Fixed (2026-04-01): `search --json` no-op; `thread --text` accepted (default text table; both commands use `conflicts_with` between `--json` and `--text`). Tests in `tests/search_fts.rs`. |
