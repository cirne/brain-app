node_modules/
dist/
.env
.env.local
*.p8
*.db
*.db-shm
*.db-wal
.DS_Store
*.tsbuildinfo
# Multi-tenant durable root: tenants (`usr_…/`), `.global/`, and **ripmail** (indexed mail, maildir, SQLite).
# Do not commit — millions of lines come from `ripmail/.../maildir/` under each tenant.
/data/
# Legacy name; same contents as `data/` until you rename/remove the folder
/data-multitenant/
# Eval home + zip cache (see eval/README.md)
data-eval/
# Per-case wiki vaults from wiki JSONL evals (see eval/README.md)
.data-eval/
# Local wiki tree (markdown); not part of this app repo
/wiki/
coverage/
target/
# Linux ripmail ELF staged for Docker COPY (npm run docker:ripmail:build)
.docker/linux-ripmail/
desktop/binaries/ripmail-*
desktop/src-tauri/bin/cloudflared-*
desktop/resources/server-bundle/
*.log
# Claude Code / local assistant config (e.g. settings.local.json)
.claude/
