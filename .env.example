WIKI_DIR=/Users/yourname/brain
AUTH_USER=lew
AUTH_PASS=changeme
ANTHROPIC_API_KEY=
RIPMAIL_BIN=ripmail
PORT=3000

# Optional: append-only agent wiki edit log (default ./data/wiki-edits.jsonl; use /data/wiki-edits.jsonl on Fly)
# WIKI_EDIT_HISTORY_PATH=

# Calendar — ICS feed URLs (copy from Google Calendar settings > "Secret address in iCal format")
CIRNE_TRAVEL_ICS_URL=
LEW_PERSONAL_ICS_URL=

# Docker / local container testing
# Absolute path to local wiki/brain repo (mounted into container at /wiki)
LOCAL_WIKI_DIR=/Users/cirne/brain
