# Local markdown wiki root (directory on disk; not a git remote)
WIKI_DIR=/Users/yourname/brain
AUTH_USER=lew
AUTH_PASS=changeme
ANTHROPIC_API_KEY=
RIPMAIL_BIN=ripmail
# After `npm run ripmail:dev`, you can point at the workspace binary (optional — `npm run dev` sets this when the debug artifact exists; same path as `cargo metadata` target_directory + /debug/ripmail):
# RIPMAIL_BIN=/absolute/path/to/target/debug/ripmail
# Container: ripmail data lives under /ripmail (Dockerfile ENV). Local dev: defaults to ~/.ripmail if unset
# RIPMAIL_HOME=/ripmail

# Non-interactive ripmail setup (container start.sh when config.json is missing). Gmail: use an app password.
RIPMAIL_EMAIL_ADDRESS=
RIPMAIL_IMAP_PASSWORD=
# Used by ripmail setup validation and optional ripmail LLM features (see docs/ARCHITECTURE.md)
OPENAI_API_KEY=
PORT=3000

# Optional: append-only agent wiki edit log (default ./data/wiki-edits.jsonl under cwd)
# WIKI_EDIT_HISTORY_PATH=

# Calendar — ICS feed URLs (copy from Google Calendar settings > "Secret address in iCal format")
CIRNE_TRAVEL_ICS_URL=
LEW_PERSONAL_ICS_URL=

# Docker: `start.sh` ensures /wiki exists (empty or bind-mounted). Ripmail uses /ripmail inside the image (no volume).

# Tauri release: derive key for embedding allowlisted *_API_KEY values from this file (or export BRAIN_EMBED_MASTER_KEY).
# BRAIN_EMBED_MASTER_KEY=
