WIKI_DIR=/Users/cirne/brain
AUTH_USER=lew
AUTH_PASS=changeme
ANTHROPIC_API_KEY=
RIPMAIL_BIN=ripmail
PORT=3000

# Calendar — ICS feed URLs (copy from Google Calendar settings > "Secret address in iCal format")
CIRNE_TRAVEL_ICS_URL=
LEW_PERSONAL_ICS_URL=
# Local cache dir for parsed ICS events (use /data/calendar on Fly.io)
CALENDAR_CACHE_DIR=./data/calendar
